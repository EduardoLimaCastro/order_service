spring:
  application:
    name: order_service

  datasource:
    url: jdbc:postgresql://localhost:5435/order_service
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8083